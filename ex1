import datetime
currentyear = datetime.datetime.now().year

while True:
  age = int(input("What is your age?"))
  alltext =""
  text = "Your will turn 100 in the year of "+str(int(currentyear)+ (100-age))
  alltext += str(text)
  print (alltext)
